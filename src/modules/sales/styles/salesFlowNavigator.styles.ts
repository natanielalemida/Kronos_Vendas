import {StyleSheet} from 'react-native';

import {colors} from '@/modules/styles';

export const salesFlowNavigatorStyles = StyleSheet.create({
  headerButton: {
    marginRight: 16,
  },
  compactIcon: {
    padding: 10,
  },
  confirmIcon: {
    padding: 10,
    marginBottom: 10,
  },
});

export const salesFlowNavigatorHeaderStyles = {
  backgroundColor: colors.arcGreen,
};
